# Change Log

## 0.2.1

### CLI Changes

- Add two new commands `tepkit vasp check` and `tepkit vasp check-dirs`
- Remove two obsolete commands `tepkit thirdorder check` and `tepkit fourthorder check-dirs`
  (now use `tepkit vasp check-dirs` instead)

### Miscellaneous

- Add `logger.raw` as a shortcut for `logger.opt(raw=True)`
